C
C*************************   CLOSIO ************************************
C
C  close the input and output files
C
C***********************************************************************
C
      SUBROUTINE CLOSIO
C
      IMPLICIT NONE
      INCLUDE 'IOUNITS.H'
C
      CLOSE(IFN)
      CLOSE(IIN1)
      CLOSE(IIN2)
      CLOSE(IIN3)
      CLOSE(IIN4)
      CLOSE(IIN5)
      CLOSE(IIN6)
      CLOSE(IIN7)
      CLOSE(IIN8)
      CLOSE(IIN9)
      CLOSE(IIN10)
      CLOSE(IIN11)
      CLOSE(IIN16)
      CLOSE(IIN17)
      CLOSE(IIN18)
      CLOSE(IIN20)
      CLOSE(IIN21)
      CLOSE(IIN22)
      CLOSE(IIN23)
      CLOSE(IIN25)
      CLOSE(IIN26)
      CLOSE(IIN27)
      CLOSE(IIN28)
      CLOSE(IIN29)
      CLOSE(IIN30)
      CLOSE(IIN31)
      CLOSE(IIN32)
      CLOSE(IIN33)
      CLOSE(IIN34)
      CLOSE(IIN35)
      CLOSE(IIN36)
      CLOSE(IIN37)
      CLOSE(IIN38)
      CLOSE(IIN39)
      CLOSE(IIN50)
      CLOSE(IIN51)
      CLOSE(IIN60)
      CLOSE(IIN61)
      CLOSE(IIN62)
      CLOSE(IIN63)
      CLOSE(IIN64)
      CLOSE(IOUT1)
      CLOSE(IOUT2)
      CLOSE(IOUT3)
      CLOSE(IOUT4)
      CLOSE(IOUT5)
      CLOSE(IOUT6)
      CLOSE(IOUT7)
      CLOSE(IOUT8)
      CLOSE(IOUT9)
      CLOSE(IOUT10)
      CLOSE(IOUT11)
      CLOSE(IOUT12)
      CLOSE(IOUT13)
      CLOSE(IOUT14)
      CLOSE(IOUT15)
      CLOSE(IOUT16)
      CLOSE(IOUT17)
      CLOSE(IOUT18)
      CLOSE(IOUT19)
      CLOSE(IOUT20)
      CLOSE(IOUT30)
      CLOSE(IOUT31)
      CLOSE(IOUT32)
      CLOSE(IOUT36)
      CLOSE(IOUT40)
      CLOSE(IOUT41) 
      CLOSE(IOUT42) 
      CLOSE(IOUT43)
      CLOSE(IOUT44)
      CLOSE(IOUT50) 
      CLOSE(IOUT51) 
      CLOSE(IOUT57)
      CLOSE(IOUT80)
      CLOSE(IOUT81)
      CLOSE(IOUTPT)
      CLOSE(ITERM)
C
      RETURN
      END
